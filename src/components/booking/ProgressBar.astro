---
interface Props {
  currentStep: number;
  totalSteps: number;
}

const { currentStep, totalSteps } = Astro.props;
const progress = (currentStep / totalSteps) * 100;
---

<div class="wizard-progress mb-12">
  <div class="flex justify-between mb-4">
    {Array.from({ length: totalSteps }, (_, i) => i + 1).map(step => (
      <div class="flex flex-col items-center flex-1">
        <div 
          class={`w-16 h-16 rounded-none flex items-center justify-center font-bold text-2xl transition-all duration-300 ${
            step < currentStep 
              ? 'bg-gold text-black' 
              : step === currentStep 
              ? 'bg-gold text-black ring-8 ring-gold/30' 
              : 'bg-white/10 text-white/40'
          }`}
        >
          {step < currentStep ? '✓' : step}
        </div>
        <span class="text-[10px] uppercase tracking-[0.2em] mt-3 opacity-60 font-headline">
          {step === 1 ? 'Type' : step === 2 ? 'Placement' : 'Details'}
        </span>
      </div>
    ))}
  </div>
  
  <div class="h-1 bg-white/10 rounded-none overflow-hidden">
    <div 
      class="h-full bg-gradient-to-r from-gold to-amber-500 transition-all duration-500 ease-out"
      style={`width: ${progress}%`}
    ></div>
  </div>
</div>
